grades:
    scratch:
        type: float
        calculation: (correctness / 5.0 * 9 + 1).round(1)
        hide_calculated: true
    hello:
        type: float
        subgrades:
            correctness: integer
            design: integer
            style: integer
        automatic:
            correctness: (check_score * 5).round
            style: style_score
        calculation: ((3 * correctness + 2 * design + 1 * style) / 30.0 * 9 + 1).round(1)
        hide_calculated: true
    water:
        type: float
        subgrades:
            correctness: integer
            design: integer
            style: integer
        automatic:
            correctness: (check_score * 5).round
            style: style_score
        calculation: ((3 * correctness + 2 * design + 1 * style) / 30.0 * 9 + 1).round(1)
        hide_calculated: true
    mario_less:
        type: float
        subgrades:
            correctness: integer
            design: integer
            style: integer
        automatic:
            correctness: (check_score * 5).round
            style: style_score
        calculation: ((3 * correctness + 2 * design + 1 * style) / 30.0 * 9 + 1).round(1)
        hide_calculated: true
    mario_more:
        type: float
        subgrades:
            correctness: integer
            design: integer
            style: integer
        automatic:
            correctness: (check_score * 5).round
            style: style_score
        calculation: ((3 * correctness + 2 * design + 1 * style) / 30.0 * 9 + 1).round(1)
        hide_calculated: true
    cash:
        type: float
        subgrades:
            correctness: integer
            design: integer
            style: integer
        automatic:
            correctness: (check_score * 5).round
            style: style_score
        calculation: ((3 * correctness + 2 * design + 1 * style) / 30.0 * 9 + 1).round(1)
        hide_calculated: true
    credit:
        type: float
        subgrades:
            correctness: integer
            design: integer
            style: integer
        automatic:
            correctness: (check_score * 5).round
            style: style_score
        calculation: ((3 * correctness + 2 * design + 1 * style) / 30.0 * 9 + 1).round(1)
        hide_calculated: true
    initials_less:
        type: float
        subgrades:
            correctness: integer
            design: integer
            style: integer
        calculation: ((3 * correctness + 2 * design + 1 * style) / 30.0 * 9 + 1).round(1)
        hide_calculated: true
    initials_more:
        type: float
        subgrades:
            correctness: integer
            design: integer
            style: integer
        calculation: ((3 * correctness + 2 * design + 1 * style) / 30.0 * 9 + 1).round(1)
        hide_calculated: true
    caesar:
        type: float
        subgrades:
            correctness: integer
            design: integer
            style: integer
        calculation: ((3 * correctness + 2 * design + 1 * style) / 30.0 * 9 + 1).round(1)
        hide_calculated: true
    vigenere:
        type: float
        subgrades:
            correctness: integer
            design: integer
            style: integer
        calculation: ((3 * correctness + 2 * design + 1 * style) / 30.0 * 9 + 1).round(1)
        hide_calculated: true
    crack:
        type: float
        subgrades:
            correctness: integer
            design: integer
            style: integer
        calculation: ((3 * correctness + 2 * design + 1 * style) / 30.0 * 9 + 1).round(1)
        hide_calculated: true
    tentamen1:
        type: float
        subgrades:
            points: integer
        calculation: (points / 31.0 * 9 + 1).round(1)
        hide_calculated: false
    tentamen2:
        type: float
        subgrades:
            points: integer
        calculation: (points / 32.0 * 9 + 1).round(1)
        hide_calculated: false
    hertentamen:
        type: float
        subgrades:
            points: integer
        calculation: (points / 32.0 * 9 + 1).round(1)

calculation:
    resit:
        hertentamens: 40
        psets: 60
    normal:
        tentamens: 40
        psets: 60

psets:
    minimum: 5.5
    submits:
        hello: 1

tentamens:
    minimum: 5.5
    submits:
        tentamen1: 1
        tentamen2: 1

hertentamens:
    minimum: 5.5
    submits:
        hertentamen: 1
